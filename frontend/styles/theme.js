import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#187BCD",
      contrastText: "#FFFFFF"
    },
    secondary: {
      main: "#2A9DF4",
    },
  },
});

export default theme